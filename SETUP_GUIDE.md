# إعداد مفاتيح API لمنظومة دهب دكتور

## الخطوة 1: تحديث ملف .env.local

قم بفتح ملف `.env.local` في المجلد الرئيسي للمشروع وأضف المفاتيح التالية:

```env
# OpenAI API Key (GPT-4o) - المحرك الأساسي
OPENAI_API_KEY=your_openai_api_key_here

# OpenRouter API Key (GLM-5.2) - المحرك البديل
OPENROUTER_API_KEY=your_openrouter_api_key_here

# Google Gemini API Key (اختياري - كنسخة احتياطية)
GEMINI_API_KEY=your_gemini_api_key_here
```

## الخطوة 2: تثبيت الحزم الجديدة

تم بالفعل تثبيت حزمة `openai`، تأكد من عدم وجود أخطاء:

```bash
npm install
```

## الخطوة 3: تشغيل التطبيق

```bash
npm run dev
```

## المحركات المتاحة

المنظومة الآن تدعم 4 محركات ذكاء اصطناعي:

1. **OpenAI GPT-4o** - المحرك الأساسي والأقوى (الأولوية القصوى)
2. **OpenRouter GLM-5.2** - محرك بديل متقدم (الأولوية الثانية)
3. **Google Gemini 1.5 Flash** - محرك احتياطي (الأولوية الثالثة)
4. **المحرك المحلي** - يعمل بدون إنترنت (نسخة احتياطية دائمة)

## نظام التبديل الذكي

- المنظومة تختار تلقائياً أفضل محرك متاح بناءً على الأولوية
- يمكنك اختيار المحرك يدوياً من واجهة التطبيق
- في حالة فشل أي محرك، ينتقل تلقائياً للمحرك التالي
- المحرك المحلي يعمل دائماً كنسخة احتياطية

## الميزات الجديدة

✅ دعم OpenAI GPT-4o
✅ دعم OpenRouter مع نموذج GLM-5.2
✅ واجهة اختيار المحرك AI
✅ نظام تبديل ذكي بين المحركات
✅ دعم الصور مع جميع المحركات
✅ بث حي للنتائج (Streaming)
✅ حفظ تلقائي في ملفات JSON

## التطوير القادم

🔄 تحسين قاعدة بيانات الآيسيهات
🔄 إضافة مخططات هندسية متكاملة
🔄 ربط جميع المكونات مع بعضها
🔄 ميزات متقدمة للسوق الشرق الأوسط
